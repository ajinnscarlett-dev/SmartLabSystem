<Window x:Class="SmartLab.Client.AdminLoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SmartLab - Admin Login"
        Height="450"
        Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Margin="30">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- TITLE -->
        <TextBlock Grid.Row="0"
                   Text="SmartLab Admin"
                   FontSize="28"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,20,0,30"/>

        <!-- USERNAME -->
        <StackPanel Grid.Row="1"
                    Margin="0,0,0,15">

            <TextBlock Text="Admin Username"
                       FontWeight="SemiBold"
                       Margin="0,0,0,5"/>

            <TextBox x:Name="UsernameTextBox"
                     Height="35"
                     Padding="8"/>
        </StackPanel>

        <!-- PASSWORD -->
        <StackPanel Grid.Row="2"
                    Margin="0,0,0,20">

            <TextBlock Text="Password"
                       FontWeight="SemiBold"
                       Margin="0,0,0,5"/>

            <PasswordBox x:Name="PasswordBox"
                         Height="35"
                         Padding="8"/>
        </StackPanel>

        <!-- LOGIN BUTTON -->
        <Button Grid.Row="3"
                x:Name="LoginButton"
                Content="Admin Login"
                Height="40"
                FontWeight="Bold"
                Click="LoginButton_Click"/>

        <!-- STATUS -->
        <TextBlock Grid.Row="4"
                   x:Name="StatusText"
                   Text=""
                   TextAlignment="Center"
                   TextWrapping="Wrap"
                   Margin="0,20,0,0"/>

    </Grid>

</Window>