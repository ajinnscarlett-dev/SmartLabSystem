<Window x:Class="SmartLab.Client.AssistanceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Need Assistance"
        Width="450"
        Height="310"
        MinWidth="420"
        MinHeight="290"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource SmartLabPageBackground}"
        Foreground="{StaticResource SmartLabText}"
        FontFamily="Segoe UI">
    <Border Margin="18"
            Padding="18"
            Background="{StaticResource SmartLabSurface}"
            BorderBrush="{StaticResource SmartLabBorder}"
            BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel>
                <TextBlock Text="NEED ASSISTANCE" FontSize="18" FontWeight="SemiBold"/>
                <TextBlock Text="Tell your teacher/MIS what you need help with." FontSize="12" Foreground="{StaticResource SmartLabMuted}" Margin="0,4,0,12"/>
            </StackPanel>

            <ComboBox x:Name="CategoryComboBox"
                      Grid.Row="1"
                      Height="34"
                      SelectedIndex="0"
                      Margin="0,0,0,10">
                <ComboBoxItem Content="General"/>
                <ComboBoxItem Content="Computer / Hardware"/>
                <ComboBoxItem Content="Network / Internet"/>
                <ComboBoxItem Content="Software"/>
                <ComboBoxItem Content="Account / Login"/>
            </ComboBox>

            <TextBox x:Name="DescriptionTextBox"
                     Grid.Row="2"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     MinHeight="105"
                     Padding="8"
                     VerticalContentAlignment="Top"/>

            <StackPanel Grid.Row="3"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,12,0,0">
                <Button Content="CANCEL"
                        Width="85"
                        Height="32"
                        Margin="0,0,8,0"
                        Click="CancelButton_Click"/>
                <Button Content="SEND REQUEST"
                        Width="112"
                        Height="32"
                        Style="{StaticResource SmartLabPrimaryButton}"
                        Click="SendButton_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
