<Window x:Class="SmartLab.Client.UserManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SmartLabSystem — User Management"
        Width="1260" Height="780" MinWidth="1040" MinHeight="620"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource SmartLabPageBackground}">
    <Grid Margin="20">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,14">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Students &amp; Staff" Style="{StaticResource SmartLabPageTitle}"/>
                <TextBlock Text="Manage SmartLab accounts, roles, and access state." Style="{StaticResource SmartLabCaption}" Margin="0,3,0,0"/>
            </StackPanel>
            <Button x:Name="CloseButton" Grid.Column="1" Content="BACK" Style="{StaticResource SmartLabBackButton}" Click="CloseButton_Click"/>
        </Grid>

        <Border Grid.Row="1" Style="{StaticResource SmartLabToolbarBorder}" Margin="0,0,0,12" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <Grid Margin="0,0,10,0">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                    <TextBlock Text="SEARCH" FontSize="9" FontWeight="SemiBold" Foreground="{StaticResource SmartLabMuted}" VerticalAlignment="Center"/>
                    <TextBox x:Name="SearchBox" Grid.Column="1" Margin="10,0,0,0" Height="34" ToolTip="Search username, student number, name, role, or status" TextChanged="SearchBox_TextChanged"/>
                </Grid>
                <Button Grid.Column="1" Content="IMPORT STUDENTS" Width="142" Height="34" Margin="6,0,0,0" Style="{StaticResource SmartLabPrimaryButton}" Click="ImportStudentsButton_Click"/>
                <Button Grid.Column="2" Content="ADD ACCOUNT" Width="112" Height="34" Margin="6,0,0,0" Click="AddButton_Click"/>
                <Button Grid.Column="3" Content="REFRESH" Width="82" Height="34" Margin="6,0,0,0" Click="RefreshButton_Click"/>
            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource SmartLabSectionBorder}" Padding="0">
            <DataGrid x:Name="UsersGrid" Style="{StaticResource SmartLabDataGrid}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding UserId}" Width="58"/>
                    <DataGridTextColumn Header="STUDENT NO." Binding="{Binding StudentNumber}" Width="140"/>
                    <DataGridTextColumn Header="NAME" Binding="{Binding FullName}" Width="1.7*"/>
                    <DataGridTextColumn Header="USERNAME" Binding="{Binding Username}" Width="1.2*"/>
                    <DataGridTextColumn Header="ROLE" Binding="{Binding Role}" Width="100"/>
                    <DataGridTextColumn Header="STATUS" Binding="{Binding Status}" Width="95"/>
                    <DataGridTextColumn Header="PASSWORD STATUS" Binding="{Binding PasswordStatus}" Width="135"/>
                    <DataGridTextColumn Header="CREATED" Binding="{Binding CreatedAt, StringFormat={}{0:yyyy-MM-dd HH:mm}}" Width="145"/>
                    <DataGridTemplateColumn Header="ACTIONS" Width="350">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="USERNAME" Width="78" Height="28" Margin="2" Tag="{Binding}" Click="RenameButton_Click"/>
                                    <Button Content="ROLE" Width="52" Height="28" Margin="2" Tag="{Binding}" Click="RoleButton_Click"/>
                                    <Button Content="RESET" Width="58" Height="28" Margin="2" Tag="{Binding}" Click="PasswordButton_Click"/>
                                    <Button Content="DISABLE" Width="65" Height="28" Margin="2" Tag="{Binding}" Click="DeleteButton_Click"/>
                                    <Button Content="ENABLE" Width="60" Height="28" Margin="2" Tag="{Binding}" Click="RestoreButton_Click"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Grid Grid.Row="3" Margin="2,10,0,0">
            <TextBlock x:Name="StatusText" Text="Ready" Style="{StaticResource SmartLabCaption}" VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</Window>